import { ModelMapperService } from './model-mapper.service';

describe('ModelMapperService', () => {
  it('should be defined', () => {
    const service = new ModelMapperService();
    expect(service).toBeDefined();
  });
});
